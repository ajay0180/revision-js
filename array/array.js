const arr = [1,2,3,4,5,6];

const sub_arr = arr.slice(0, 4); // upto index 3;

// console.log(sub_arr);           

// console.log(arr.slice(0, -3));  // upto before 3rd last elem

console.log(arr.indexOf(4, 0, 1));

